# Multi Classifier Tester


Test multiple classification models and return their performance metric values efficiently.

## Installation

You can install the package via pip:

```bash
pip install all-classifier-uygar-final-v3
